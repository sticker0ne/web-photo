export const state = () => ({
  connected: false
})

export const getters = {}

export const mutations = {
  setCameraConnected(state, payload) {
    state.connected = payload
  }
}

export const actions = {}
